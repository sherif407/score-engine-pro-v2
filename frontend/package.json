{
  "name": "score-engine-pro",
  "private": true,
  "version": "2.0.1",
  "type": "module",

  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build --force",
    "preview": "npx vite preview"
  },

  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "axios": "^1.6.0",
    "react-router-dom": "^6.22.0"
  },

  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.2.0"
  },

  "engines": {
    "node": "24.x"
  }
}
